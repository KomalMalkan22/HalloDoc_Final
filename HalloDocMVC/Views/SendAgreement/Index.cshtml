@{
    Layout = "";
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Review Agreement</title>
    <link rel="stylesheet" href="~/css/main.css" asp-append-version="true" />
</head>
<body>
    <div class="d-flex flex-column justify-content-center mx-auto" style="max-width:500px;">

        <p>To provide best medical service, we cannot determine the cost right away. If you agree to our service, so we will provide care and follow-up until all care is completed. So with this points, if you like us to provide care to you click on "Agree" and we'll get started immediately, if you do not agree simply click "Cancel".</p>

        <div class="d-flex justify-content-around">
            <form asp-controller="SendAgreement" asp-action="Accept">
                <input type="hidden" name="RequestID" value="@TempData["RequestID"]" />
                <button type="submit" class="btn btn-success">I Agree</button>
            </form>            
            <button type="button" class="btn btn-danger" data-bs-toggle="modal" data-bs-target="#cancelAgreement">Cancel</button>
        </div>
    </div>

    <div class="modal fade" id="cancelAgreement" tabindex="-1" role="dialog" aria-labelledby="exampleModalCenterTitle" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered" role="document">
            <div class="modal-content">
                <div class="modal-header bg-info text-white">
                    <h5 class="modal-title" id="exampleModalLongTitle">Cancel Confirmation</h5>
                    <i class="fa-solid fa-xmark" data-bs-dismiss="modal"></i>
                </div>
                <form method="post" asp-controller="SendAgreement" asp-action="Reject">
                    <div class="modal-body">
                        <input type="hidden" value="@TempData["RequestID"]" name="RequestId" />
                        <p class="text-secondary">@TempData["PatientName"]</p>
                        <div class="col-12">
                            <div class="form-floating mb-3">
                                <textarea name="Notes" class="form-control shadow-none" id="Reason" placeholder="Leave a comment here" style="height: 100px"></textarea>
                                <label for="Reason"><small>Please provide a brief explaination for your cancelation</small></label>
                            </div>
                        </div>
                    </div>
                    <div class="modal-footer">
                        <button type="submit" class="btn btn-info text-white shadow-none">Confirm</button>
                        <button type="button" class="btn border-info shadow-none" data-bs-dismiss="modal">Cancel</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
    <script src="~/js/site.js" asp-append-version="true"></script>
    <script src="~/js/bootstrap.bundle.min.js" asp-append-version="true"></script>
</body>
</html>
